#include "main.h"

/**
 * _islower - check c if its lower case
 * author: indiya Reuben
 * Return: 1 if statement is true and 0 if not
 * _islower
 */

int _islower(int c)

{
	if (c >= 'a' && c <= 'z')
		return (l);

	else return(0);
}

