#include "main.h"

/**
 * mul - this will multiply 2 numbers
 * Return: the result
 * @a: this is the first number to multiply
 * @b: is the second number
 */

int mul(int a, int b)
{
	return (a * b);
}
